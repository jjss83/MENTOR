{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "mentor-api: watch",
      "type": "shell",
      "command": "dotnet watch --project mentor-api run",
      "options": { "cwd": "${workspaceFolder}" },
      "group": "build",
      "problemMatcher": "$msCompile",
      "presentation": { "reveal": "always", "panel": "dedicated" }
    }
  ]
}
